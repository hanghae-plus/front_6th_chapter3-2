import { describe, it, expect } from "vitest";
import { subject } from "./subject";

describe("subject", () => {
  it("does X for simplest case", () => {
    // Arrange

    // Act

    // Assert
    expect(/* actual */).toEqual(/* expected */);
  });
});
